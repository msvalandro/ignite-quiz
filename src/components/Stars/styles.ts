import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
  canvas: {
    height: 249,
    width: 257,
    position: 'absolute',
    zIndex: 1,
  },
})
